# Core RAG dependencies
pymupdf>=1.23.0            # PDF parsing (fitz)
sentence-transformers>=2.7.0  # Local embeddings (BAAI/bge-small-en-v1.5)
faiss-cpu>=1.8.0           # Vector similarity search

# LLM providers (install at least one)
anthropic>=0.28.0          # Claude API
openai>=1.30.0             # OpenAI / OpenAI-compatible APIs

# UI
gradio>=4.37.0

# Utilities
python-dotenv>=1.0.0       # Load .env file
numpy>=1.24.0
